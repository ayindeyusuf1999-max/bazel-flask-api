name: Bazel CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install Bazel
        uses: bazel-contrib/setup-bazel@0.8.1
        with:
          bazelisk-version: 1.x

      - name: Install Python deps
        run: pip install flask

      - name: Build with Bazel
        run: bazel build //app:server

      - name: Test with Bazel
        run: bazel test //tests:test_main --test_output=all
